/* SmartMIrror
 * Utils
 * MIT Licensed.
 */
const colors = require("colors/safe");

module.exports = {
	colors: {
		warn: colors.yellow,
		error: colors.red,
		info: colors.blue,
		pass: colors.green
	}
};
